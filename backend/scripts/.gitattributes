# Git LFS Tracking Configuration

# Track all CSV files in results directories
results/**/*.csv filter=lfs diff=lfs merge=lfs -text

# Track all JSON files in results directories  
results/**/*.json filter=lfs diff=lfs merge=lfs -text

# Track large metrics export files
metrics_export/**/*.csv filter=lfs diff=lfs merge=lfs -text
